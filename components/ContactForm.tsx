'use client';
import {useState} from 'react';
export default function ContactForm({labels}:{labels:any}){const [status,setStatus]=useState('');async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setStatus('Sending…');const data=Object.fromEntries(new FormData(e.currentTarget));const r=await fetch('/api/appointments',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(data)});const j=await r.json().catch(()=>({}));setStatus(r.ok?'Request received. We’ll follow up soon.':j.error||'Please try again.');if(r.ok)e.currentTarget.reset();}
return <form className="form-card" onSubmit={submit}><div className="field-grid"><label>{labels.formName}<input name="name" required/></label><label>{labels.formEmail}<input name="email" type="email" required/></label><label>{labels.formPhone}<input name="phone"/></label><label>{labels.formArea}<input name="area" placeholder="Toronto / GTA"/></label></div><label>{labels.formGoal}<input name="goal" required/></label><label>{labels.formService}<select name="service" required><option value="in-home">In-home personal training</option><option value="private">Private personal training</option><option value="online">Online coaching</option><option value="programming">Workout programming</option><option value="nutrition">Nutrition coaching</option></select></label><label>{labels.formMessage}<textarea name="message" rows={5}/></label><button className="btn" type="submit">{labels.formSubmit}</button>{status&&<p className="form-status" aria-live="polite">{status}</p>}</form>}
