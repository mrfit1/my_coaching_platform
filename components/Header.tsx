import Link from 'next/link';import {getCopy,locales,type Locale} from '@/lib/i18n';import {getSiteSettings} from '@/lib/cms';
export default async function Header({lang}:{lang:Locale}){const t=getCopy(lang);const settings=await getSiteSettings();const brand=(typeof settings.brand_name==='string'&&settings.brand_name)||(process.env.NEXT_PUBLIC_BRAND_NAME||'Toronto Performance Coaching');return <header className="site-header"><div className="container nav"><Link className="brand" href={`/${lang}`}><span className="brand-mark">TP</span><span>{brand}</span></Link><nav><Link href={`/${lang}/services`}>{t.services}</Link><Link href={`/${lang}/exercises`}>{t.exercises}</Link><Link href={`/${lang}/about`}>{t.about}</Link><Link href={`/${lang}/pricing`}>{t.pricing}</Link><Link href={`/${lang}/contact`}>{t.contact}</Link></nav><div className="nav-actions"><div className="langs">{locales.map(l=><Link key={l} className={l===lang?'active':''} href={`/${l}`}>{l.toUpperCase()}</Link>)}</div><Link className="btn small" href={`/${lang}/login`}>{t.login}</Link></div></div></header>}
