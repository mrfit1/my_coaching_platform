import {NextResponse} from 'next/server';import {z} from 'zod';import {createClient} from '@supabase/supabase-js';
const schema=z.object({name:z.string().min(2).max(100),email:z.string().email(),phone:z.string().max(40).optional(),area:z.string().max(100).optional(),goal:z.string().min(2).max(250),service:z.string().min(2).max(100),message:z.string().max(2000).optional()});
export async function POST(req:Request){try{const parsed=schema.safeParse(await req.json());if(!parsed.success)return NextResponse.json({error:'Please check the form fields.'},{status:400});const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)return NextResponse.json({error:'Booking storage is not configured yet. Add Supabase keys in Vercel.'},{status:503});const db=createClient(url,key,{auth:{persistSession:false}});const {error}=await db.from('appointment_requests').insert(parsed.data);if(error)throw error;return NextResponse.json({ok:true});}catch{return NextResponse.json({error:'Unable to submit request.'},{status:500})}}
