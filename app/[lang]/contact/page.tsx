import ContactForm from '@/components/ContactForm';import {getCopy,isLocale,type Locale} from '@/lib/i18n';import {localeMetadata} from '@/lib/seo';import type {Metadata} from 'next';import {notFound} from 'next/navigation';
export async function generateMetadata({params}:{params:Promise<{lang:string}>}):Promise<Metadata>{const {lang}=await params;if(!isLocale(lang))return{};return localeMetadata(lang,'/contact')}
export default async function Contact({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!isLocale(lang))notFound();const l=lang as Locale,t=getCopy(l);return <><section className="page-hero"><div className="container"><div className="kicker">GET STARTED</div><h1>{t.contactTitle}</h1><p className="lead">{t.contactBody}</p></div></section><section className="section"><div className="container split"><div><h2>Toronto & selected GTA areas</h2><p className="lead">In-home sessions can be requested by neighbourhood. Private and online coaching are also available.</p><div className="credential-grid"><div className="metric"><strong>4</strong><span>Languages</span></div><div className="metric"><strong>1:1</strong><span>Personal coaching</span></div><div className="metric"><strong>Flexible</strong><span>Booking formats</span></div></div></div><ContactForm labels={t}/></div></section></>}
