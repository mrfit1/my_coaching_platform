import {getCopy,isLocale,type Locale} from '@/lib/i18n';import {getSupabaseServer} from '@/lib/supabase-server';import {notFound,redirect} from 'next/navigation';import Link from 'next/link';
export const dynamic='force-dynamic';
export default async function Dashboard({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!isLocale(lang))notFound();const l=lang as Locale,t=getCopy(l);const supabase=await getSupabaseServer();let email='';if(supabase){const {data}=await supabase.auth.getUser();if(!data.user)redirect(`/${l}/login`);email=data.user.email||'';}return <section className="dashboard"><div className="container"><div className="section-head"><div className="kicker">CLIENT PORTAL</div><h1>{t.dashTitle}</h1>{email&&<p>{email}</p>}</div><div className="dash-grid"><div className="dash-card"><h2>{t.nextSession}</h2><p className="muted">{t.empty}</p><Link className="btn small" href={`/${l}/dashboard/book`}>Book a session</Link></div><div className="dash-card"><h2>{t.planLabel}</h2><p className="muted">{t.empty}</p></div><div className="dash-card"><h2>{t.nutritionLabel}</h2><p className="muted">{t.empty}</p></div><div className="dash-card"><h2>{t.progressLabel}</h2><p className="muted">{t.empty}</p></div><div className="dash-card"><h2>{t.messagesLabel}</h2><p className="muted">{t.empty}</p></div></div></div></section>}
