import ContactForm from '@/components/ContactForm';import {getCopy,isLocale,type Locale} from '@/lib/i18n';import {getSupabaseServer} from '@/lib/supabase-server';import {notFound,redirect} from 'next/navigation';
export const dynamic='force-dynamic';
export default async function Book({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!isLocale(lang))notFound();const l=lang as Locale,t=getCopy(l);const supabase=await getSupabaseServer();if(supabase){const {data}=await supabase.auth.getUser();if(!data.user)redirect(`/${l}/login`)}return <section className="section soft"><div className="container"><div className="section-head"><div className="kicker">CLIENT BOOKING</div><h1>{t.contactTitle}</h1><p>{t.contactBody}</p></div><ContactForm labels={t}/></div></section>}
