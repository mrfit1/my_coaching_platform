import BookingForm from '@/components/BookingForm';import {isLocale} from '@/lib/i18n';import {requireUser} from '@/lib/auth';import {notFound} from 'next/navigation';
export const dynamic='force-dynamic';
export default async function Book({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!isLocale(lang))notFound();await requireUser(lang);return <section className="dashboard"><div className="container narrow"><div className="section-head"><div className="kicker">CLIENT BOOKING</div><h1>Request a training session</h1><p>Select your preferred time and format. Requests are confirmed by the coaching team.</p></div><BookingForm/></div></section>}
