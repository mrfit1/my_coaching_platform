import LoginPanel from '@/components/LoginPanel';import {getCopy,isLocale,type Locale} from '@/lib/i18n';import {localeMetadata} from '@/lib/seo';import type {Metadata} from 'next';import {notFound} from 'next/navigation';
export async function generateMetadata({params}:{params:Promise<{lang:string}>}):Promise<Metadata>{const {lang}=await params;if(!isLocale(lang))return{};return {...localeMetadata(lang,'/login'),robots:{index:false,follow:false}}}
export default async function Login({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!isLocale(lang))notFound();const l=lang as Locale,t=getCopy(l);return <section className="section soft"><div className="container login-wrap"><div className="section-head"><div className="kicker">CLIENT PORTAL</div><h1>{t.loginTitle}</h1><p>{t.loginBody}</p></div><LoginPanel t={t} lang={l}/></div></section>}
