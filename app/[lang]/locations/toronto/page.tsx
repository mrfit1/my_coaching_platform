import type {Metadata} from 'next';import Link from 'next/link';import {notFound} from 'next/navigation';import {getCopy,isLocale,type Locale} from '@/lib/i18n';import {localeMetadata,siteUrl} from '@/lib/seo';
export async function generateMetadata({params}:{params:Promise<{lang:string}>}):Promise<Metadata>{const {lang}=await params;if(!isLocale(lang))return{};return localeMetadata(lang,'/locations/toronto')}
export default async function Toronto({params}:{params:Promise<{lang:string}>}){const {lang}=await params;if(!isLocale(lang))notFound();const l=lang as Locale,t=getCopy(l);const schema={'@context':'https://schema.org','@type':'Service',name:'Personal Training in Toronto',areaServed:{'@type':'City',name:'Toronto',containedInPlace:{'@type':'AdministrativeArea',name:'Ontario'}},provider:{'@id':`${siteUrl}/#business`}};return <><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(schema)}}/><section className="page-hero"><div className="container"><div className="kicker">TORONTO, ONTARIO</div><h1>{l==='fa'?'پرسنال ترینینگ در تورنتو':'Personal training across Toronto'}</h1><p className="lead">{l==='fa'?'تمرین در منزل، فضای خصوصی و کوچینگ آنلاین برای مشتریان در تورنتو و برخی مناطق GTA.':'In-home, private and online coaching for clients across Toronto and selected GTA areas.'}</p></div></section><section className="section"><div className="container page-grid"><div className="service-detail"><h2>In-home convenience</h2><p>Request sessions at your home or condo gym. Final travel availability depends on neighbourhood, schedule and trainer availability.</p></div><div className="service-detail"><h2>Private or online options</h2><p>If in-home training is not the best fit, choose private one-to-one sessions or a fully online plan.</p></div></div><div className="container" style={{marginTop:30}}><Link className="btn" href={`/${l}/contact`}>{t.cta}</Link></div></section></>}
